# Common descriptions
locals {
  project_id_param_description   = "The ID of the project."
  conn_param_description         = "Name of GitLab connection to use. If not provided, the default GitLab connection will be used."
}
